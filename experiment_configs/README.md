# Console Screenshots — Input Parameters & 3D Viewer

Dump zone for screenshots of Bio Discovery console content that isn't downloadable (each experiment's
**Input Parameters** panel, and **3D structure viewer** images for the target-complex folds). Captured
2026-07-16 night as a hedge against loss of console access.

**Sorting (tomorrow):** every Input Parameters shot has the **experiment name** visible in its header,
which maps directly to the ledger in `../CORPUS_MANIFEST.md`. Correlate by name → rename/move each into
the matching `../experiment_N/` folder. (Claude can read the images and do the correlation.)

**What should be here:**
- Input Parameters panel for the 9 completed experiments (+ the 2 failed, if easy) — the authoritative
  backup for `../INPUT_PARAMETERS.md` (which is reconstructed from session memory; these verify it).
- 3D **structure PDBs ARE downloadable** (candidate → Structure Preview → Download; named
  `<candidateID>_boltz2.pdb`, B-factor column = per-residue pLDDT). These belong in the matching
  `experiment_N/` folder, not here — file by matching the candidate ID to the CSV `id` column. Grab the
  target-complexes at least: Run 1 (nanobody–HER2), exp_8 (humanized trastuzumab–HER2), exp_11 (evolved
  trastuzumab–HER2, already filed). A 3D-viewer *screenshot* is still a nice portfolio image on top of the PDB.

Names on the console correlate with the 11-row experiment list; see `../CORPUS_MANIFEST.md` for the full
console-name → dir mapping.

__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.env
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
*.log

# macOS
.DS_Store

# Excel lock files
~$*.xlsx

# Bio Discovery saved-page render-asset sidecars (CSS/JS/images) — archived compressed
# as "HTML Extracts/render-assets.tar.gz". The scrapers only read the .html files, so the
# raw sidecars are redundant in git; keep them locally, restore from the tarball if needed.
HTML Extracts/*_files/
